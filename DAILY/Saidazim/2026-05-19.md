---
date: 2026-05-19
developer: Saidazim
---

# 📅 2026-05-19 — Saidazim

## Sessions
## Decisions
## Bugs
## Ideas
## Fixes
## TODOs

### ✅ [weWatch] Android import timing race — CDN URL wait in importMedia
> Root cause: backend extracts at 1.5s -> shows import bar. XHR/perf scan fires at 3s. User taps at 2s -> PAGE_URL imported instead of CDN URL -> WatchParty re-extract fails -> iframe mode.

Fix #20: importMedia now polls up to 3s (100ms interval) when backendFoundVideoRef=true AND cleanUrlFoundRef=false before proceeding. Resolves immediately if CDN URL already confirmed. effective = detectedMediaRef.current (CDN URL) instead of media (PAGE_URL).

File: apps/mobile/src/hooks/useMediaDetection.ts — importMedia function.
> Fixed: 2026-05-19 23:24 | By: Emirhan

### ✅ [weWatch] Android proxy fallback re-sync — lastExecutedSyncRef in handlePlayerReady
> Root cause: CDN segments fail -> ExoPlayer switches to HLS proxy source -> new load -> handlePlayerReady fires -> playerReadyRef.current=true -> early return -> NO re-sync. Proxy starts from 0s paused; isPlaying=false -> drift correction skips (!isPlaying).

Fix #21: Added lastExecutedSyncRef ref — executeSync saves each sync into it. handlePlayerReady removed the 'if (playerReadyRef.current) return' guard. Now uses pendingSyncRef ?? lastExecutedSyncRef — re-syncs on proxy fallback. Also sets intendedPlayingRef.current = syncToExecute.isPlaying so BUFFER_START is emitted correctly during post-proxy buffering. room?.videoUrl change resets lastExecutedSyncRef to null (new video = fresh state).

File: apps/mobile/src/hooks/useWatchPartyRoom.ts — handlePlayerReady + executeSync.
> Fixed: 2026-05-19 23:24 | By: Emirhan

### ✅ [weWatch] Android sync jerkiness — rate correction removed, hard-seek only drift
> Rate correction был слишком медленным: 1.02x за 5с = 0.1с/heartbeat. Для дрейфа 1.5с нужно 15 циклов x 5с = 75 секунд. Фактически sync не работал 6+ минут.

Fix (round 2 пересмотр Fix #16): Rate correction удалён полностью. DRIFT_FORCE_SEEK_SECS=2.0 — только hard seeks. Drift effect: обнаружил дрейф > 2с -> suppressBuffer 5с + suppressDrift 8с + isSyncing.current=true 2с -> seekTo(expected). Без rate adjust констант.

Также Fix #17 (executeSync suppressDrift 8с) остался нетронутым — важен для предотвращения cascade после VIDEO_PLAY/PAUSE/SEEK sync.

File: apps/mobile/src/hooks/useWatchPartyRoom.ts — drift correction useEffect.
> Fixed: 2026-05-19 23:24 | By: Emirhan
